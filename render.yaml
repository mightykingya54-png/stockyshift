services:
  - type: web
    name: stockyshift
    env: node
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /healthz
    # Persistent disk for SQLite database (required — without it, all data is lost on deploy)
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
      - key: SHOPIFY_API_KEY
        sync: false
      - key: SCOPES
        value: write_inventory,read_inventory,read_products,read_locations
      - key: SHOPIFY_API_SECRET
        sync: false
      - key: SHOPIFY_APP_URL
        value: https://stockyshift.com
      - key: SESSION_SECRET
        generateValue: true
      - key: PORT
        value: 10000
      - key: APP_URL
        value: https://stockyshift.com
      - key: DB_PATH
        value: /data/stockyshift.db
      - key: SESSION_DB_PATH
        value: /data/stockyshift_sessions.db
      # Production: Resend SMTP (update after creating account + verifying domain)
      - key: SMTP_HOST
        value: smtp.resend.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        value: resend
      - key: SMTP_PASS
        sync: false
      - key: FROM_EMAIL
        value: stockyshift@stockyshift.com
      # Contact email addresses (set up forwarding at your domain registrar)
      - key: CONTACT_PRIVACY
        value: privacy@stockyshift.com
      - key: CONTACT_LEGAL
        value: legal@stockyshift.com
      # Billing: set to "false" for real charges. Use "true" while in development.
      - key: BILLING_TEST_MODE
        value: "false"
      # Explicitly disable dev-only billing bypass in production (defense in depth)
      - key: SKIP_BILLING
        value: "false"
      # Install notification — gets an email every time someone installs StockyShift
      - key: NOTIFY_EMAIL
        value: stockyshift@stockyshift.com
